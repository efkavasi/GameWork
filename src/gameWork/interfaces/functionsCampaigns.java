package gameWork.interfaces;

import gameWork.Entities.Campaigns;

public interface functionsCampaigns {
	void save(Campaigns campaign);
	void delete(Campaigns campaign);
	void update(Campaigns campaign);
	
}
